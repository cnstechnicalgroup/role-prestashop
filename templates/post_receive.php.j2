<?php
  // Post receive hook script
  if ( $_SERVER['HTTP_X_GITLAB_EVENT'] == 'Push Hook' ) {
    shell_exec( 'cd {{ web_root }} && git reset --hard HEAD && git pull && rm cache/class_index.php' );
    header("Status: 200");
    echo 'OK';
  } else {
    header("Status: 400");
    echo 'NOT FOUND';
  }
?>
